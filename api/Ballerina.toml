[package]
org = "brainlabs"
name = "backend"
version = "1.0.0"

[build-options]
observabilityIncluded = true

# Note: HuggingFace API calls are made via plain http:Client in main.bal
# (no typed connector needed). Remove the line below if it causes
# bal pull errors, or leave it for future typed connector usage.
# [dependencies]
# "avi0ra/huggingface" = "0.2.1"
